id: AB-003
scenario: skill-overlap
request: "Multiple skills cover the same capability"
expected:
  required_outputs:
    - "one primary skill"
    - "rejected alternatives"
    - "reason"
forbidden:
  - "simultaneous overlapping primary skills"
