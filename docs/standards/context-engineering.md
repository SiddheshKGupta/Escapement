# Context Engineering

Use:

```text
Write → Select → Compress → Isolate
```

Store durable state in repository files. Load only the active task, required
standards, selected skills, and direct evidence. Use fresh contexts for
independent research, review, or execution when context rot becomes material.
